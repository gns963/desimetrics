@AGENTS.md

# Semantic-SEO content methodology

Before writing or rewriting any prose content (blog posts, or the intro/explainer/FAQ prose blocks
on calculator/directory pages — not the calculator UI itself), read
`~/Projects/semantic-seo-content-system/projects/bijlicalc/CLAUDE.md` first. It's the adapted
core-layer project file (this site uses `-core` only, no `-local` overlay) and points at the actual
writing contract and rule registry, which live one level up in that sibling repo.
